# RSS-notifications-Artemis-1


#install necessary packages
    pip install plyer
    pip install beautifulsoup4
    pip install requests
    pip install time

#how to run 
    right click on notifiyer.pyw file go to open with find your python folder and select pythonw to run it with if successful it should show a notification about Artemis